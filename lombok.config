config.stopBubbling = true

# NullAway/Error Prone 이 Lombok 생성 코드를 검사 대상에서 제외하도록 표시한다.
# 생성 코드의 위반은 소스가 없어 고칠 수 없고, 섞이면 진짜 위반이 묻힌다 (docs/decisions/0001).
# pitest 도 같은 표시를 보고 생성 코드를 변이 대상에서 뺀다 — 지우면 게터·equals 에
# 의미 없는 변이가 쏟아진다.
lombok.addLombokGeneratedAnnotation = true
